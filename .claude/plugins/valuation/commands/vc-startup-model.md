---
description: Value high-growth or pre-profit companies using VC and ARR methods
argument-hint: "[company]"
---

Load the `vc-startup-model` skill and produce a valuation range with key drivers.

If the company is not provided, ask for it.

Example:
`/vc-startup-model Series B SaaS`
